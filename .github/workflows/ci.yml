name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    # Tests only exercise pure-Python parsing/import logic (manifest + module
    # load), so a Linux runner is sufficient even though the server itself
    # only runs against a Windows/macOS DaVinci Resolve installation.
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@0b93645e9fea7318ecaed2b359559ac225c90a2b # v5.3.0
        with:
          python-version: "3.11"

      - name: Install dependencies (mcp only -- heavy ML deps are imported lazily)
        run: pip install "mcp==1.28.1"

      - name: Run tests
        run: python -m unittest discover -s tests -v
